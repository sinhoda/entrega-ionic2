import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.duoc.ANunez',
  appName: 'PresenteProfe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
